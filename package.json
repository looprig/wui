{
  "name": "looprig-wui-workspace",
  "private": true,
  "workspaces": [
    "packages/*",
    "app"
  ],
  "scripts": {
    "build": "npm run build --workspace app",
    "dist:reset": "npm run dist:reset --workspace app"
  }
}
