{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://looprig.dev/sessionwire/v1/hostlink_error_runtime_mismatch.schema.json",
  "title": "HostLinkErrorRuntimeMismatch",
  "description": "Strict typed HostLink refusal that names the safe compatible runtime boundary and no runtime payload.",
  "type": "object",
  "additionalProperties": false,
  "required": ["code", "runtime_compatibility_id"],
  "properties": {
    "code": {"const": "runtime_mismatch"},
    "runtime_compatibility_id": {"type": "string", "minLength": 1, "description": "Opaque UTF-8 compatibility identity; Core enforces the 256-byte limit."}
  }
}
